import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: true,
  template: `<header class="p-4 bg-gray-900 text-white">
    <h1>Multiplica</h1>
  </header>`
})
export class HeaderComponent {}